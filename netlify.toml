[build]
  command = "npm install --no-package-lock --no-audit --no-fund && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--no-package-lock --no-audit --no-fund"
